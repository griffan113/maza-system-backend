type WithPaginationResponse {
  metadata: PaginationMetadata
  data: [Client]
}

type Query {
  indexClients(
    paginationRequestDTO: PaginationRequestDTO
    filter: String
  ): WithPaginationResponse
}
