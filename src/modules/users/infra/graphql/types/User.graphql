scalar DateTime
scalar EmailAddress

enum UserRole {
  ADMIN
  PRODUCTION
  EDITOR
}

type User {
  id: String!
  email: EmailAddress!
  role: UserRole!
  name: String!
  password: String!
  created_at: DateTime!
  updated_at: DateTime!
}
