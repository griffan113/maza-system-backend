scalar DateTime
scalar EmailAddress
scalar UUID

enum UserRole {
  ADMIN
  PRODUCTION
  EDITOR
}

type User {
  id: UUID!
  email: EmailAddress!
  role: UserRole!
  name: String!
  password: String!
  created_at: DateTime!
  updated_at: DateTime!
}
