type WithPaginationUsersResponse {
  metadata: PaginationMetadata
  data: [User]
}

type Query {
  indexUsers(
    paginationRequestDTO: PaginationRequestDTO
    filter: String
  ): WithPaginationUsersResponse
}
