input UpdateUserDTO {
  user_id: String
  name: String
  email: String
  role: UserRole
  password: String
  old_password: String
}

type Mutation {
  updateUser(updateUserDTO: UpdateUserDTO!): User
}
