input UpdateUserDTO {
  user_id: String
  name: String
  email: String
  is_admin: Boolean
  password: String
  old_password: String
}

type Mutation {
  updateUser(updateUserDTO: UpdateUserDTO!): User
}
